import React, {Component} from 'react';

export class SearchBox extends Component {
  render() {
    return (
      <section>
        <input type="text"/>
        <button>Search</button>
      </section>
    );
  }
}